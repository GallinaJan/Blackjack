#include "player.h"
#include <cstdlib>

Cards give_card(){
    int number = rand() % 52 + 1;
    Cards rand_card;
    for (auto cards_list : elem){
        if (elem.give_number() = number){
            rand_card = elem;
        }
    }
    return rand_cart;
}